---
interface Props {
    title: string;
    required?: boolean;
}

const { title, required = false } = Astro.props;
---

<div>
    <label class="block mb-2.5 font-bold"
        >{title}{required && <span class="text-yellow-400">*</span>}</label
    >
    <slot />
</div>
